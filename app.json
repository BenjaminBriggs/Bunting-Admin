{
  "name": "Bunting Admin",
  "description": "Self-hosted feature flag dashboard for iOS/macOS apps",
  "repository": "https://github.com/BenjaminBriggs/Bunting-Admin",
  "logo": "https://raw.githubusercontent.com/BenjaminBriggs/Bunting-Admin/main/public/images/Logotype.png",
  "keywords": ["feature-flags", "nextjs", "ios", "macos", "swift"],
  "env": {
    "NODE_ENV": {
      "description": "Environment for the application",
      "value": "production"
    },
    "NEXTAUTH_SECRET": {
      "description": "Random secret for JWT signing",
      "generator": "secret"
    },
    "NEXTAUTH_URL": {
      "description": "App URL for NextAuth callbacks (auto-set by Heroku)",
      "required": false
    },
    "DATABASE_URL": {
      "description": "Set automatically by Heroku Postgres",
      "required": false
    },
    "S3_ENDPOINT": {
      "description": "Optional: S3-compatible storage endpoint for config publishing",
      "required": false
    },
    "S3_ACCESS_KEY_ID": {
      "description": "Optional: S3 access key for config publishing",
      "required": false
    },
    "S3_SECRET_ACCESS_KEY": {
      "description": "Optional: S3 secret key for config publishing",
      "required": false
    },
    "S3_BUCKET": {
      "description": "Optional: S3 bucket name for config publishing",
      "required": false
    },
    "S3_REGION": {
      "description": "Optional: S3 region for config publishing",
      "required": false,
      "value": "us-east-1"
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "basic"
    }
  },
  "addons": ["heroku-postgresql:essential-0"],
  "buildpacks": [{ "url": "heroku/nodejs" }],
  "scripts": {
    "postdeploy": "npx prisma migrate deploy"
  },
  "success_url": "/setup"
}
